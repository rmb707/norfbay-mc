# Secrets
.env
*.secret
secrets/
deploy/*.local
deploy/TARGET.local

# Server world data & backups (large, machine-local)
data/

# Built modpack artifacts
dist/
*.mrpack
# ...but keep the packs embedded in the launcher (needed to build the .exe)
!launcher/assets/*.mrpack

# Node / build
node_modules/
.next/
out/
build/
launcher/dist/
launcher/out/

# OS / editor
.DS_Store
Thumbs.db
*.log
