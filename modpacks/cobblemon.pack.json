{
  "name": "Norfbay Cobblemon",
  "summary": "Pokémon in Minecraft — Cobblemon with curated addons, performance, QoL and richer worldgen.",
  "minecraft": "1.21.1",
  "loader": "fabric",
  "java": 21,
  "serverAddress": "cobblemon.norfbay.com",
  "note": "side: both = client+server (content/worldgen must match). client = render/UI only. server = perf/admin. Cobblemon addons are validated for 1.21.1 at build time; incompatible ones are skipped, not fatal.",
  "mods": [
    { "slug": "fabric-api",            "side": "both",   "required": true },
    { "slug": "fabric-language-kotlin","side": "both",   "required": true },
    { "slug": "architectury-api",      "side": "both",   "required": true },
    { "slug": "cobblemon",             "side": "both",   "required": true },

    { "slug": "cobblemon-integrations","side": "both",   "required": false },
    { "slug": "megamons",              "side": "both",   "required": false },
    { "slug": "cobblemon-mega-showdown","side": "both",  "required": false },
    { "slug": "myths-and-legends",     "side": "both",   "required": false },
    { "slug": "cobbreeding",           "side": "both",   "required": false },
    { "slug": "cobblenav",             "side": "both",   "required": false },

    { "slug": "terralith",             "side": "both",   "required": false },
    { "slug": "waystones",             "side": "both",   "required": false },

    { "slug": "lithium",               "side": "both",   "required": true },
    { "slug": "ferrite-core",          "side": "both",   "required": true },
    { "slug": "krypton",               "side": "server", "required": false },
    { "slug": "spark",                 "side": "server", "required": false },
    { "slug": "chunky",                "side": "server", "required": false },

    { "slug": "sodium",                "side": "client", "required": true },
    { "slug": "iris",                  "side": "client", "required": false },
    { "slug": "entityculling",         "side": "client", "required": false },
    { "slug": "immediatelyfast",       "side": "client", "required": false },
    { "slug": "modmenu",               "side": "client", "required": true },
    { "slug": "reeses-sodium-options", "side": "client", "required": false },
    { "slug": "jade",                  "side": "client", "required": false },
    { "slug": "roughly-enough-items",  "side": "client", "required": false },
    { "slug": "appleskin",             "side": "both",   "required": false },
    { "slug": "xaeros-minimap",        "side": "client", "required": false },
    { "slug": "xaeros-world-map",      "side": "client", "required": false }
  ]
}
