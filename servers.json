{
  "_note": "The launcher fetches this at runtime to resolve server addresses, so moving the servers (laptop -> Oracle -> playit) is a one-line edit here + git push, with NO new .exe. Values are playit.gg Minecraft-Java tunnel hostnames (SRV-backed, so no port needed) that front the Oracle box's localhost-bound servers.",
  "cobblemon": { "server": "expected-champion.gl.joinmc.link" },
  "vanilla": { "server": "classes-adopted.gl.joinmc.link" }
}
